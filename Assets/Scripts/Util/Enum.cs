namespace Assets.Scripts.Util
{
    public class Enum
    {
        public enum Direction { Up, Down, Left, Right, None }
        public enum HitboxType
        {
            Attack,
            Character,
            Mech,
            Enemy
        }
    }
}
